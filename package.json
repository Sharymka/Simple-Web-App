{
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-session": "^1.18.0",
    "morgan": "^1.10.0",
    "mysql2": "^3.11.2"
  },
  "scripts": {
    "prestart": "cd web && npm run build",
    "start": "node server.js",
    "prestart:dev": "cd web && npm run build",
    "start:dev": "nodemon server.js"
  },
  "devDependencies": {
    "nodemon": "^3.1.6",
    "concurrently": "^9.0.1"
  }
}
